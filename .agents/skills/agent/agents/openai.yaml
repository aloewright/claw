interface:
  display_name: "Agent"
  short_description: "Repo-specific patterns and workflows for agent"
  default_prompt: "Use the agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true